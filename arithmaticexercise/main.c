#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <math.h>
#include <string.h>
#include "head.h"
#define D(x) ((x)*10)
#define MAX(a,b) (a>b? a:b)

int main(void)
{
    printf("%d",010);
    //char input[20];
    //gets(input);
    //char *ch=cutstr(input,0);
    //puts(ch);
    //turnstrtonum(ch);
    //turnstrtonum("123");
    return 0;
}


#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <string.h>
#define USERNAME "VSTVR"
#define PASSWORD "wanqiwei668899"
#define Key 5
int main()
{







   /*char str[]="For the hole";
   char*ptr_str="For the hole";
   printf("字符串常量的地址：%p\n","For the hole");
   printf("字符数组的首地址：%p\n",str);
   printf("字符指针的取值是：%p",ptr_str);
   /*printf("%p",*"你好，北京。");


   /* char*word="My heart is still";
    word+=9;
    puts(word);

   /* char password[100];
    printf("请输入一个密码:");
    gets(password);
    encrypt(password);
    printf("加密后的密码是：%s",password);


   /* char str1[20] ,str2[20];
    gets(str1);gets(str2);
    strcat(str1,str2);     //将第二个字符串拼接到第一个字符串上
    printf("%s\n",str1);
    printf("%s",str2);


    /*char username[25],password[50];char start;
    start:printf("用户名：");gets(username);
    printf("密码：");gets(password);
    if(login(username,password))
    {
        printf("密码正确！");
    }else{
        printf("登录失败!请重新输入！\n");
        goto start;
    }


    /*char name1[]={'H','\0','f','1'};     //1     见\0则止
    char name2[]="helloworld";          //10
    char name3[]="你好";                //4      汉字=2长度，标点符号也算2个长度
    printf("name1的长度是：%d\n",strlen(name1));
    printf("name2的长度是：%d\n",strlen(name2));
    printf("name3的长度是：%d",strlen(name3));


    /*char name[]={'j','a','c','k','\0'};
    int length=GetLength(name);
    printf("%d",length);*/
    return 0;
}
